// Literals

// JS defines two typs of values:
// 1: fixed values are called literals
// 2: variable values are called variables

// Literals include arrays, numbers, booleans, objects, and strings

let days = ['Mon', 'Tues', 'Wed', 'Thurs', 'Fri'];
console.log(days.toString());

console.log(days.length);

let tired = true;

let soup = {
    a: 'chicken noodle',
    b: 'tomato',
    c: 'beef and barley'
};

console.log(soup.a);

let car = 'Honda';

let decimal = 10.2;

